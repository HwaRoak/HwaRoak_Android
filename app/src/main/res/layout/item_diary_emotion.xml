<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="5dp"
    android:id="@+id/diary_recycler_layout"
    android:background="@color/colorFrameBackground"
    android:layout_marginVertical="2dp"
    android:layout_marginHorizontal="2dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    
    <ImageView
        android:id="@+id/diary_recycler_imv"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/ic_emotion1"
        android:scaleType="fitCenter"
        android:paddingVertical="2dp"
        android:paddingHorizontal="3dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/diary_recycler_tv"
        />

    <TextView
        android:id="@+id/diary_recycler_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="차분한"
        android:layout_marginTop="5dp"
        android:gravity="center"
        style="@style/freesentation5_11sp_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/diary_recycler_imv"/>

</androidx.constraintlayout.widget.ConstraintLayout>